# Admin app
# Backend API base (no trailing slash).
# Default if omitted: /api/v1 (recommended with Vite proxy + Nginx)
VITE_API_BASE_URL=/api/v1

# Google Sign-In
VITE_GOOGLE_CLIENT_ID=

# tRPC endpoint base (recommended same-origin)
# Default if omitted: /api/trpc
VITE_TRPC_URL=/api/trpc
